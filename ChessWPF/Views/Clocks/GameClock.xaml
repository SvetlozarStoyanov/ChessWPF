<UserControl x:Class="ChessWPF.Views.Clocks.GameClock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChessWPF.Views.Clocks"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="100"
             Loaded="GameClock_Loaded">
    
    <Grid Background="White" VerticalAlignment="Top" SizeChanged="Grid_SizeChanged">

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="timeTextBlock" Grid.Column="0" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" Text="{Binding TimeLeft,UpdateSourceTrigger=PropertyChanged}">

        </TextBlock>
        <TextBlock x:Name="colorTextBlock" Grid.Column="1" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" Text="{Binding Color}">
            
        </TextBlock>
    </Grid>
</UserControl>
